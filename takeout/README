in fst directory:

make
python ../scripts/reap_multiwords.py ../../apertium-en-es/apertium-en-es.en.metadix | ./transducer2


generates fst's from dictionary


if make clean doesn't work:
shopt -s extglob
